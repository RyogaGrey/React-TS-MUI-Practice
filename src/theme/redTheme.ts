import { createTheme } from '@mui/material/styles';

const redTheme = createTheme({
  palette: {
    mode: 'light',
    primary: {
      main: '#ff1744',
    },
    secondary: {
      main: '#f50057',
    },
  },
});

export default redTheme;