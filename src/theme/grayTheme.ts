// src/themes/grayTheme.ts
import { createTheme } from '@mui/material/styles';

const grayTheme = createTheme({
  palette: {
    mode: 'dark',
    primary: {
      main: '#607d8b',
    },
    secondary: {
      main: '#b0bec5',
    },
  },
});

export default grayTheme;
